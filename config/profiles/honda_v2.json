{
  "profile_id": "honda_v2",
  "extends": "generic_v1",
  "derived_from": "Auto-derived from honda/cr-v-2026/ivi via derive_layout, reviewed 2026-09-04 -- see docs/HANDOVER.md same date. figure_min_width/height_pt removed: the auto-derived value (282.98x177.13pt) excluded every real content figure in the Features chapter, whose actual figures are portrait screen-mockup composites around 194x336pt -- one whole-document widest-gap threshold picked a boundary above them (same failure mode already documented for honda_v1). Falls back to generic_v1's 40x40pt default instead, confirmed well below every real content figure measured in this chapter (from ~130pt icon composites up to the 194x336pt mockups) and well above lone inline icons (~2-25pt). heading_prefixes:[\"■\"] added same day, matching honda_v1 -- CR-V uses the identical \"■\" sub-heading marker convention (confirmed real: \"■To make a call using the imported phonebook\", \"■Favorite Contacts\"), and derive_layout's draft never sets this field (it isn't derived automatically), so it was silently missing until reviewed here. column_detect_per_page:true added same day (docs/ARCHITECTURE.md \"17.\"): the Features chapter is 1-column overall but individual pages have a real local sidebar (e.g. an icon-meaning legend running parallel to the main procedure column, p.316) that a plain columns=1 read never reorders, scattering the sidebar's text into the wrong place. Deliberately NOT enabled by default for every manual (tested directly against Subaru/Honda Pilot and found to change already-reviewed output unpredictably) -- CR-V has no reviewed baseline to protect, so it opts in here specifically. page_number_offset:-1 added 2026-09-08 (same fix as honda_v1/Pilot): derive_layout never sets this field either, so it silently defaulted to generic_v1's +1, printing every citation 2 pages ahead of the real page footer (confirmed real: the \"Start Up\" figure cited p.261, but its own page footer reads 259) -- found while investigating an unrelated caption bug the user reported for this exact function, see docs/ARCHITECTURE.md 2026-09-08. running_head_separator_font:\"HONDACommon\" was TRIED same day to also clean up that same page_citation field (CR-V's margin breadcrumb uses the identical HONDACommon glyph-reuse trick as Pilot, confirmed via a per-character font dump) but reverted immediately: unlike Pilot, setting it here changes section_source=bookmarks' actual splitting behavior (build_blocks_from_running_head_breadcrumbs is tried FIRST, ahead of the font-heading path this chapter's already-reviewed 23-function structure depends on) -- confirmed by direct regenerate, 23 functions dropped to 21 with real functions missing (\"About Your Audio System\") and one garbled (\"Bluetooth\\u00ae\\u00ae\"). Left unset; the page_citation glyph is a separate, still-open cosmetic issue (Thresholds tab only, does not affect published Markdown) -- see docs/ARCHITECTURE.md 2026-09-08 \"27.\" for the reverted attempt and what to try instead.",
  "layout": {
    "columns": 1,
    "section_source": "bookmarks",
    "header_boundary_pt": 20,
    "footer_boundary_pt": 395,
    "heading_prefixes": ["■"],
    "column_detect_per_page": true,
    "page_number_offset": -1
  }
}